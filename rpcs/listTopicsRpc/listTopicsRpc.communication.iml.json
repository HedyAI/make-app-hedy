{
    "url": "/topics",
    "method": "GET",
    "response": {
        "iterate": "{{body.data}}",
        "output": {
            "label": "{{item.name}}",
            "value": "{{item.id}}"
        }
    }
}
