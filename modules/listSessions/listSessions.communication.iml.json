{
    "url": "/sessions",
    "method": "GET",
    "qs": {
        "limit": "{{if(ifempty(parameters.limit, 10) > 100, 100, ifempty(parameters.limit, 10))}}"
    },
    "response": {
        "output": "{{item}}",
        "iterate": "{{body.data}}",
        "limit": "{{ifempty(parameters.limit, 10)}}"
    },
    "pagination": {
        "qs": {
            "after": "{{body.pagination.next}}"
        },
        "condition": "{{body.pagination.hasMore}}"
    }
}
