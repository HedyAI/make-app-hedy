{
  "name": "make-app-hedy-tests",
  "private": true,
  "scripts": {
    "test": "jest --runInBand",
    "test:verbose": "jest --runInBand --verbose"
  },
  "devDependencies": {
    "dotenv": "^16.4.0",
    "jest": "^29.7.0"
  }
}
