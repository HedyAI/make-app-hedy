{
    "url": "/webhooks",
    "method": "POST",
    "body": {
        "url": "{{webhook.url}}",
        "events": "{{parameters.events}}"
    },
    "response": {
        "data": {
            "webhookId": "{{body.data.id}}"
        }
    }
}
